// Core class
export { SwiperCore } from './core';

// React bindings
export { Swiper, default } from './components/Swiper';
export type { SwiperProps, SwiperRef } from './components/Swiper';

// Types
export type { SwiperOptions, SwiperState, ResolvedSwiperOptions } from './types';

// Utilities
export { lerp, damp, symmetricMod, calculateForce, normalize } from './utils/math';
